{
    "name": "functions",
    "description": "Firebase Cloud Functions for credores",
    "private": true,
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "lint": "echo \"No lint configured\"",
        "serve": "firebase emulators:start --only functions,firestore",
        "deploy": "firebase deploy --only functions"
    },
    "engines": {
        "node": "18"
    },
    "dependencies": {
        "firebase-admin": "^11.11.0",
        "firebase-functions": "^4.4.0"
    },
    "devDependencies": {
        "typescript": "~5.7.2",
        "esbuild": "^0.19.3",
        "@types/node": "^20.5.6"
    }
}